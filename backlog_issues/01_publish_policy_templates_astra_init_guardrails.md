    # Publish policy templates: `astra init guardrails`

    Goal: reduce adoption friction with a runnable starter project.

Acceptance criteria:
- Generates `policy.astra.json`, `tests.json`, `README.md`
- Works on Windows/macOS/Linux
- CI runs `astra format` + `astra test` on template

    ---
    **Labels:** adoption, demo  
    **Milestone:** M1 — Drop-in Adoption
